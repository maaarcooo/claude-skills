interface:
  display_name: "Resume Task"
  short_description: "Verify a handoff and continue the work"
  default_prompt: "Use $resume to load the latest handoff, verify it, and continue the work."
policy:
  allow_implicit_invocation: false
